import React from "react";

function Instructions () {
    return (
        <div className="instructions">
    <p>Instructions: Click a character to earn points, but don't click on any image more than once!</p>
    </div>
    )
}

export default Instructions;